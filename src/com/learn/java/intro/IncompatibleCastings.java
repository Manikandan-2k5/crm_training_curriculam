package com.learn.java.intro;


/**
 * <p>This class provides examples for incompatible type castings</p>
 */
public class IncompatibleCastings {
    public static void main(String[] args) {
        String string = "Hello";
        // int num = (int)string; //cannot cast from String to int
        // boolean bool = (boolean)5; //cannot cast from int to boolean
        // int num1 = (int)true; //cannot cast from boolean to int
    }
}
